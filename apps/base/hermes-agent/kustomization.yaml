apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pvc.yaml
  - secret.sops.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
# SSH access to node3/laptop removed 2026-08-21 - agent is chat/webui only
# again, no host access. ANTHROPIC_API_KEY / OPENAI_API_KEY also still
# not set (Nous Portal free tier in use instead).
