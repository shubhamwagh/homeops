# Exists but is never bound to hermes-critical-operator by default - see
# clusterrole-critical.yaml. A ServiceAccount with no binding has zero
# effective permissions; this is safe to have present at rest.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hermes-critical-operator
  namespace: hermes-agent
