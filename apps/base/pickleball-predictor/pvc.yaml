apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pickleball-predictor-data
  namespace: pickleball-predictor
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
