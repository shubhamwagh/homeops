# k3s cluster config — contains secrets
infrastructure/metal/inventory.yml
infrastructure/metal/group_vars/all.yml
infrastructure/metal/.ansible_cache/

# age private key — NEVER commit
age.agekey

# macOS
.DS_Store

# mise
.mise.local.toml

# Temp
*.tmp
*.log
.secrets-plaintext
.secrets-plaintext-machine
.ntfy-credentials
# vps/headscale/vars.yml and inventory.yml committed encrypted via SOPS

# Hermes's own local working notes (project context it maintains for itself
# in its persistent clone) - never meant to be part of this repo. A prior
# session accidentally `git add`ed it into a commit; ignoring it here makes
# that a no-op instead of a recurring risk.
.hermes.md
