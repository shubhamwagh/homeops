apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: headplane-redirect
  namespace: headscale
spec:
  redirectRegex:
    regex: "^https://headplane\\.shublab\\.com/$"
    replacement: "https://headplane.shublab.com/admin/"
    permanent: true
