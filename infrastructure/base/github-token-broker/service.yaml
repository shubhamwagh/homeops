apiVersion: v1
kind: Service
metadata:
  name: github-token-broker
  namespace: github-token-broker
spec:
  selector:
    app: github-token-broker
  ports:
    - name: http
      port: 80
      targetPort: 8080
